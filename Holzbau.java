import java.util.LinkedList;

public abstract class Holzbau extends Dachstuhl{

	//ASSERT: int laenge must be >= 0, int amount must be >= 0
	public Holzbau(int laenge, int amount) {
		super(laenge, amount);
	}
}
