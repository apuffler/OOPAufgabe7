import java.util.LinkedList;

public abstract class Steinbau extends Dachstuhl{
	


	//ASSERT: int laenge must be >= 0, int amount must be >= 0
	public Steinbau(int laenge, int amount) {
		super(laenge, amount);
	}
}
